# DYHelper 主题图标包制作教程

本文档指导图标设计者制作一个可由 DYHelper 导入的主题图标包。底栏与右侧控件共用一个 ZIP 和一个 `manifest.json`，可在设置中统一导入、启用、禁用或删除。

图标资源格式范围为：

- PNG：静态图标，推荐作为主要格式。
- GIF：动态图标，可用于任意选中态或未选中态。
- WebP、SVG、JPEG 等格式会触发导入校验失败。

`icons` 管理底栏图标，`right_controls` 管理右侧控件图标。两部分可任选其一，也可以同时提供，但至少需要包含一项有效资源。右侧控件只替换对应静态图标节点，不会替换作者头像、旋转唱片或宿主点击与状态逻辑，因此不同作品仍显示各自的作者头像和唱片图。

## 一、先看最终结构

一个完整图标包由 `manifest.json` 以及 `icons`、`right` 中至少一个资源目录组成：

```text
my-theme-pack/
├── manifest.json
├── icons/
    ├── home_unselected.png
    ├── home_selected.png
    ├── home_unselected_dark.png
    ├── home_selected_dark.gif
    ├── friends_unselected.png
    ├── friends_selected.png
    ├── message_unselected.png
    ├── message_selected.png
    ├── profile_unselected.png
    └── profile_selected.png
└── right/
    ├── follow.png
    ├── follow_success.png
    ├── like.png
    ├── like_selected.png
    ├── comment.png
    ├── favorite.png
    ├── favorite_selected.png
    ├── share.png
    └── more.png
```

打包后的 ZIP 内部必须直接看到 `manifest.json`：

```text
my-theme-pack.zip
├── manifest.json
├── icons/
│   └── ...
└── right/
    └── ...
```

以下结构多包了一层目录，会导致应用在 ZIP 根目录找不到清单：

```text
my-theme-pack.zip
└── my-theme-pack/
    ├── manifest.json
    └── icons/
```

## 二、准备图标素材

### 1. 推荐尺寸

- 画布：`108 x 108 px`。
- 背景：透明。
- 主体安全边距：四周至少保留约 `8%`。
- 运行时显示框：最长边会等比缩放到约 `24dp`。
- 运行时位置：相对宿主原始标题位置下移 `6dp`。
- 最大尺寸：宽和高都不得超过 `512 px`。
- 单个文件：不得超过 `4 MiB`。

应用只缩放图标，不裁切图标。不同素材应统一线宽、圆角、视觉重心和留白，否则切换 Tab 时会出现跳动感。

### 2. 选中态设计

建议每个 Tab 至少绘制两张图：

- `unselected`：未选中状态，例如线框、低亮度或低饱和度图标。
- `selected`：选中状态，例如实心、高亮度或强调色图标。

若没有提供 `selected`，选中后会继续显示 `unselected`，视觉上就像状态没有变化。

### 3. GIF 建议

GIF 可以作为 `unselected`、`selected`、`unselected_dark` 或 `selected_dark` 中的任意资源。为控制宿主进程内存占用，建议：

- 尺寸不超过 `256 x 256 px`。
- 帧数不超过 `180` 帧。
- 时长不超过 `30` 秒。
- 使用透明背景并压缩颜色数量。

普通宿主底栏会播放 GIF；液态玻璃底栏会读取该 GIF 的静态帧。

## 三、编写 manifest.json

`manifest.json` 使用 UTF-8 编码，文件名固定，并位于 ZIP 根目录。键名和资源路径使用英文双引号。

### 1. 带注释讲解版

下面使用 JSONC 展示逐项注释，方便理解字段。`//` 后面的内容属于教程注释，制作实际 `manifest.json` 时删除这些注释。

```jsonc
{
  // 图标包格式版本。当前固定填写 1。
  "format_version": 1,

  // 图标包唯一 ID，用于安装、覆盖、启用和删除。
  // 仅使用英文字母、数字、点、下划线和短横线，长度 1 至 64。
  "id": "com.example.clean-bottom-bar",

  // 设置页中展示的图标包名称，内容不能为空。
  "name": "简洁线性底栏",

  // 图标包自身版本。推荐使用 主版本.次版本.修订号。
  "version": "1.0.0",

  // 作者名称。该字段可省略。
  "author": "Your Name",

  // icons 将宿主 Tab ID 映射到对应的状态资源。
  "icons": {
    // 首页或推荐 Tab。
    "homepage_home": {
      // 未选中资源为必填项，路径相对于 ZIP 根目录。
      "unselected": "icons/home_unselected.png",

      // 选中资源为可选项；省略时回退到 unselected。
      "selected": "icons/home_selected.png",

      // 深色模式未选中资源为可选项。
      "unselected_dark": "icons/home_unselected_dark.png",

      // 深色模式选中资源为可选项，也可以填写 GIF。
      "selected_dark": "icons/home_selected_dark.gif"
    },

    // 朋友 Tab。本例只提供普通明暗状态。
    "homepage_familiar": {
      "unselected": "icons/friends_unselected.png",
      "selected": "icons/friends_selected.png"
    },

    // 消息 Tab。
    "homepage_notification": {
      "unselected": "icons/message_unselected.png",
      "selected": "icons/message_selected.png"
    },

    // 个人页 Tab。
    "homepage_profile": {
      "unselected": "icons/profile_unselected.png",
      "selected": "icons/profile_selected.png"
    }
  }
}
```

### 2. 右侧控件图标

在清单顶层增加可选的 `right_controls` 对象，键名固定为以下九项，值为相对于 ZIP 根目录的
PNG 或 GIF 路径。每项都可以省略，省略后保留宿主原始图标。

```json
{
  "right_controls": {
    "follow": "right/follow.png",
    "follow_success": "right/follow_success.png",
    "like": "right/like.png",
    "like_selected": "right/like_selected.png",
    "comment": "right/comment.png",
    "favorite": "right/favorite.png",
    "favorite_selected": "right/favorite_selected.png",
    "share": "right/share.png",
    "more": "right/more.png"
  }
}
```

支持键与状态：

| 键 | 控件 | 使用条件 |
| --- | --- | --- |
| `follow` | 关注 | 未关注 |
| `follow_success` | 关注完成反馈 | 已关注/成功 |
| `like` | 点赞 | 未点赞 |
| `like_selected` | 点赞 | 已点赞 |
| `comment` | 评论 | 默认 |
| `favorite` | 收藏 | 未收藏 |
| `favorite_selected` | 收藏 | 已收藏 |
| `share` | 分享 | 默认 |
| `more` | 更多操作 | 宿主显示该控件时 |

右侧资源沿用导入限制：真实 PNG/GIF 文件头、单文件不超过 `4 MiB`、宽高不超过 `512 px`。
`right_controls` 为空或缺失时只应用 `icons`；`icons` 为空或缺失时只应用右侧控件。两者都为空会拒绝导入。

### 3. 可直接导入的标准 JSON

下面内容符合标准 JSON 语法，可以直接保存为 `manifest.json`：

```json
{
  "format_version": 1,
  "id": "com.example.clean-bottom-bar",
  "name": "简洁线性底栏",
  "version": "1.0.0",
  "author": "Your Name",
  "icons": {
    "homepage_home": {
      "unselected": "icons/home_unselected.png",
      "selected": "icons/home_selected.png",
      "unselected_dark": "icons/home_unselected_dark.png",
      "selected_dark": "icons/home_selected_dark.gif"
    },
    "homepage_familiar": {
      "unselected": "icons/friends_unselected.png",
      "selected": "icons/friends_selected.png"
    },
    "homepage_notification": {
      "unselected": "icons/message_unselected.png",
      "selected": "icons/message_selected.png"
    },
    "homepage_profile": {
      "unselected": "icons/profile_unselected.png",
      "selected": "icons/profile_selected.png"
    }
  },
  "right_controls": {
    "follow": "right/follow.png",
    "follow_success": "right/follow_success.png",
    "like": "right/like.png",
    "like_selected": "right/like_selected.png",
    "comment": "right/comment.png",
    "favorite": "right/favorite.png",
    "favorite_selected": "right/favorite_selected.png",
    "share": "right/share.png",
    "more": "right/more.png"
  }
}
```

标准 JSON 注意事项：

- 使用英文双引号，不使用中文引号。
- 最后一项后面不添加逗号。
- 实际文件中删除 JSONC 示例里的 `//` 注释。
- 建议保存为 UTF-8 且不带 BOM。

## 四、字段与状态规则

### 1. 顶层字段

| 字段 | 类型 | 必填 | 规则 |
| --- | --- | --- | --- |
| `format_version` | 整数 | 是 | 当前固定为 `1`。 |
| `id` | 字符串 | 是 | 匹配 `[A-Za-z0-9._-]{1,64}`；相同 ID 再次导入会更新原包。 |
| `name` | 字符串 | 是 | 设置页展示名称，去除首尾空格后仍需有内容。 |
| `version` | 字符串 | 是 | 图标包版本，推荐语义化版本格式。 |
| `author` | 字符串 | 否 | 设置页展示的作者名称。 |
| `icons` | 对象 | 条件必填 | Tab ID 与状态资源的映射；与 `right_controls` 至少提供一项。 |
| `right_controls` | 对象 | 条件必填 | 九个右侧控件语义键与资源路径的映射；与 `icons` 至少提供一项。 |

### 2. 单个 Tab 的状态字段

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `unselected` | 是 | 普通模式未选中资源。 |
| `selected` | 否 | 普通模式选中资源。 |
| `unselected_dark` | 否 | 深色模式未选中资源。 |
| `selected_dark` | 否 | 深色模式选中资源。 |

状态资源实际回退顺序如下：

| 当前状态 | 查找顺序 |
| --- | --- |
| 普通模式、未选中 | `unselected` |
| 普通模式、选中 | `selected` → `unselected` |
| 深色模式、未选中 | `unselected_dark` → `unselected` |
| 深色模式、选中 | `selected_dark` → `selected` → `unselected_dark` → `unselected` |

深色模式依据 Android 当前夜间模式配置判断，不对视频画面颜色进行取样。

### 3. 常用 Tab ID

| Tab ID | 常见含义 | 推荐文件前缀 |
| --- | --- | --- |
| `homepage_home` | 首页、推荐 | `home_` |
| `homepage_familiar` | 朋友 | `friends_` |
| `homepage_publish` | 发布 | `publish_` |
| `homepage_notification` | 消息 | `message_` |
| `homepage_profile` | 我、个人页 | `profile_` |
| `homepage_explore` | 探索 | `explore_` |
| `homepage_mall` | 商城 | `mall_` |
| `homepage_nearby` | 同城 | `nearby_` |

宿主存在 AB 实验或灰度 Tab 时，可把宿主返回的稳定 Tab ID 直接写入 `icons`。Tab ID 支持英文字母、数字、点、下划线、`#` 和短横线，最大长度为 128。

清单中没有匹配项的 Tab 会恢复宿主原始显示，不影响已经匹配的其他 Tab。

## 五、检查资源路径

清单中的每个资源路径都以 ZIP 根目录为起点，例如：

```jsonc
{
  // 正确：相对于 ZIP 根目录，并使用正斜杠。
  "selected": "icons/home_selected.png"
}
```

以下写法会触发校验失败：

```jsonc
{
  // 绝对路径。
  "selected": "/icons/home_selected.png",

  // 包含上级目录跳转。
  "unselected": "../home_unselected.png",

  // 文件扩展名位于格式范围之外。
  "selected_dark": "icons/home_selected.webp"
}
```

推荐统一使用：

- 小写扩展名：`.png`、`.gif`。
- ASCII 文件名：英文字母、数字、短横线和下划线。
- `/` 作为目录分隔符。
- 一个路径只对应一个 ZIP 条目，避免重复文件名。

应用会同时检查扩展名、PNG/GIF 文件头和图片尺寸。仅修改文件扩展名不会通过校验。

## 六、打包 ZIP

打包时进入图标包目录，让 `manifest.json`、`icons` 和 `right` 成为 ZIP 的第一层内容。未使用的资源目录可以省略。

查看 ZIP 内部目录：
输出开头应类似：

```text
manifest.json
icons/
icons/home_unselected.png
icons/home_selected.png
right/follow.png
right/like.png
```
### 导入限制

| 项目 | 上限或规则 |
| --- | --- |
| ZIP 文件大小 | 最大 `16 MiB` |
| ZIP 条目数量 | 最大 `64`，目录也计入条目 |
| 解压后总大小 | 最大 `32 MiB` |
| `manifest.json` | 最大 `256 KiB` |
| 单个资源 | 最大 `4 MiB` |
| 图片宽高 | 各自最大 `512 px` |
| 资源类型 | PNG 或 GIF |

ZIP 内路径会经过越界、绝对路径、`..` 和重复路径检查。导入先进入临时目录，全部校验通过后再替换正式目录，因此更新失败时原有图标包仍保持可用。

## 七、导入与实机测试

1. 打开 DYHelper 设置。
2. 进入主题图标包区域。
3. 点击“导入 ZIP 主题包”。
4. 选择制作完成的 ZIP。
5. 导入成功后，该图标包会自动启用。
6. 返回宿主首页，依次切换每个底栏 Tab。

建议逐项检查：

- 每个 Tab 都显示了正确图标。
- 选中与未选中图标会随 Tab 切换。
- PNG 边缘清晰且透明区域正常。
- GIF 在普通底栏中正常播放。
- 深色模式使用了对应 `_dark` 资源或正确回退。
- 底栏隐藏且“显示底栏文字”开启时，图标继续显示并遵循文字透明度。
- “显示底栏文字”关闭时，图标随标题槽位隐藏。
- 暂停恢复完整底栏时，图标恢复完整可见状态。
- 禁用图标包后，宿主原始文字、Drawable 和位置恢复。
- 关注、点赞、评论、收藏、分享和更多图标按清单生效，状态图标会随宿主状态切换。
- 作者头像和旋转唱片仍显示当前作品的动态内容，没有变成主题包中的固定图片。

## 八、常见问题

### 导入时提示 manifest 无效

依次检查：

1. `manifest.json` 是否位于 ZIP 根目录。
2. 实际清单是否删除了 `//` 注释。
3. `format_version` 是否为数字 `1`。
4. `id` 是否只包含允许字符。
5. `name`、`version` 是否有内容，且 `icons`、`right_controls` 至少有一项有效资源。
6. JSON 最后一项后是否残留逗号。

### 导入时提示资源无效

依次检查：

1. 清单路径与 ZIP 内文件名的大小写是否完全一致。
2. 文件格式是否为真实 PNG 或 GIF。
3. 图片宽高和文件大小是否超过限制。
4. 路径是否包含绝对路径、反斜杠或 `..`。

### 某个 Tab 仍显示宿主内容

该 Tab ID 没有匹配到 `icons`。确认宿主稳定 Tab ID，并在清单中新增对应对象。其他已匹配 Tab 会继续使用图标包。

### 切换 Tab 后图标看起来没有变化

确认该 Tab 同时提供了视觉不同的 `unselected` 和 `selected`。只提供 `unselected` 时，选中态会按规则回退到同一资源。

### 图标过小或视觉上偏移

- 检查原图四周是否存在过多透明像素。
- 把多个图标放到相同画布中统一视觉重心。
- 保持主体接近画布中心，避免仅靠修改像素画布尺寸调整位置。
- 运行时会统一限制到约 `24dp` 并向下偏移 `6dp`。

### GIF 在液态玻璃底栏中没有持续播放

这是当前渲染策略：普通底栏播放 GIF，液态玻璃底栏显示读取到的静态帧。为两种底栏提供一致观感时，建议 GIF 首帧本身就是完整图标。

## 九、发布前检查表

- [ ] ZIP 根目录直接包含 `manifest.json`。
- [ ] `manifest.json` 使用 UTF-8 标准 JSON，已删除教程注释。
- [ ] `format_version` 为 `1`。
- [ ] `id` 稳定且符合字符限制。
- [ ] 每个 Tab 都提供 `unselected`。
- [ ] 不需要底栏自定义时已省略 `icons`，并至少提供一个有效 `right_controls` 键。
- [ ] 右侧控件只使用文档列出的九个语义键。
- [ ] 需要状态变化的 Tab 同时提供 `selected`。
- [ ] 所有资源都是真实 PNG 或 GIF。
- [ ] 文件路径、大小写和清单完全一致。
- [ ] 图片尺寸、文件大小、ZIP 大小和条目数量符合限制。
- [ ] 已测试普通模式、深色模式、底栏隐藏和暂停恢复状态。
- [ ] 已测试禁用及删除后的宿主显示恢复。

图标包更新时保持相同 `id` 并提高 `version`；希望作为另一个独立图标包共存时使用新的 `id`。
